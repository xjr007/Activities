/*		Question
 *
 *		1. Create a class called [Animal]
 *
 *		2. Create 3 private variables for the [Animal] class:
 *			- String typeOfAnimal	(type of animal could be a 'lion')
 *			- String animalSound	(animal sound could be 'roar')
 *			- boolean isDangerous	(if it is dangerous, variable will be 'true')
 *
 *		3. Create a constructor for the [Animal] class:
 *			- create 3 constructor parameters
 *			- assign each parameter to a variable using 'this.'
 *				e.g. this.name = name
 *
 *		3. Create a total of 4 methods in the [Animal] class:
 *			- 3 methods returns the value of your created variables
 *			- 1 method does the following:
 *				3.1 Returns all 3 variable values
 *					e.g. return name + age + sex
 *			- each method should prefix with 'get'
 *				e.g. getMethod()
 *
 *		4. Create 3 instances of Animal called Bear, Lion, Seal.
 *
 *		5. On each instance, call all 4 methods and print the result.
 * */

class A {
	public static void main(String[] args) {
		App app = new App();
		app.run();
	}
}

class App {
	public void run() {
		// Do code here
	}
}
